import * as React from "react";
import "./Contacts.css"

const Contacts = ({ idProp }) => {
    return (
        <footer>
            <p id={idProp}>Nadia Jarana</p>

        </footer>
    )
}
export default Contacts;